CREATE TABLE BEER (
  PUNKAPI_ID BIGINT AUTO_INCREMENT PRIMARY KEY,
  NAME VARCHAR(128) NOT NULL UNIQUE,
  TAGLINE VARCHAR(256) NOT NULL,
  FIRSTBREWED VARCHAR(256) NOT NULL,
  DESCRIPTION VARCHAR(65535) NOT NULL,
  IMAGEURL VARCHAR(256) NOT NULL,
  IBU INT(32) NOT NULL
);
